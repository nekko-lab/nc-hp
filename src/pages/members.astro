---
import MemberCard from '../components/item/MemberCard.astro';
import Pagetitle from '../components/page/pagetitle.astro';
import Layout from '../layouts/Layout.astro';
import {members} from '../data/members'; 

---

<Layout title="メンバー | NC - Nekko Cloud">
  <Pagetitle title="メンバー" />
  <section class="py-16 min-h-screen">
    <div class="w-full px-8 2xl:px-24 mx-auto space-y-16">
      {members.map((member, index) => <MemberCard {...member} index={index} />)}
    </div>
  </section>
</Layout>
