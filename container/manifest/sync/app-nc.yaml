---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nekko-cloud-deployment
  labels:
    app: nekko-cloud
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nekko-cloud
  template:
    metadata:
      labels:
        app: nekko-cloud
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/port: '9113'
        prometheus.io/path: '/metrics'
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      volumes:
        - name: nginx-logs
          emptyDir: {}
      containers:
        - name: nekko-cloud-container
          image: "ghcr.io/nekko-lab/nekko_cloud.com:ec9a045bbf6c427f4fc5f96ea561cad0db266ad4" # ghcr.io/${{ github.repository }}:${{ github.sha }}
          imagePullPolicy: Always
          resources:
            limits:
              memory: '2048Mi'
              cpu: '4'
            requests:
              memory: '256Mi'
              cpu: '250m'
          volumeMounts:
            - name: nginx-logs
              mountPath: /var/log/nginx
        - name: nginx-exporter
          image: 'nginx/nginx-prometheus-exporter:1.5.1'
          args:
            - -nginx.scrape-uri=http://localhost:80/metrics
          ports:
            - containerPort: 9113
              name: metrics
          resources:
            limits:
              memory: '2048Mi'
              cpu: '2'
            requests:
              memory: '64Mi'
              cpu: '100m'
